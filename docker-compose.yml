version: '3.8'

services:
  backend:
    build: ./interview_coach
    ports:
      - "8000:8000"
    env_file: ./interview_coach/.env
    environment:
      FRONTEND_URL: https://career-mentor-companion.vercel.app
      CORS_ORIGINS: https://career-mentor-companion.vercel.app,http://localhost:5173,http://localhost:5174